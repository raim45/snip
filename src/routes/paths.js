const express = require("express")
const route = express.Router()
const calls = require

route.get("/", () =>{})
route.get("/:id", ()=>{})
route.post("/", ()=>{})
route.delete("/", ()=>{})

module.exports = route